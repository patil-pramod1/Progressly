# Email

Mailpit captures local SMTP mail without sending real messages. SMTP is exposed on `localhost:1025` and its UI on `http://localhost:8025`. Production SMTP credentials will be environment-managed, with preference/unsubscribe controls implemented alongside notification settings.

