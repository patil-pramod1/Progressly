# Kafka

Kafka is present in local Compose but not yet used by CRUD. Planned topics include `USER_CREATED`, `TASK_COMPLETED`, `GOAL_COMPLETED`, `HABIT_COMPLETED`, `REPORT_GENERATION_REQUESTED`, and `NOTIFICATION_REQUESTED`. Consumers will use consumer groups, retries, dead-letter handling, and idempotency keys before production activation.

