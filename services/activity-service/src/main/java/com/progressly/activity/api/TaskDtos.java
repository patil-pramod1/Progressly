package com.progressly.activity.api; import com.progressly.activity.domain.Task; import jakarta.validation.constraints.*; import java.time.*; import java.util.UUID;
public final class TaskDtos{private TaskDtos(){} public record Request(@NotBlank @Size(max=180)String title,@Size(max=2000)String description,String category,String priority,LocalDate dueDate,LocalTime dueTime,@Min(0)Integer estimatedMinutes,@Min(0)Integer actualMinutes,UUID goalId,UUID milestoneId,Task.Recurrence recurrence,Task.Status status){} public record StatusRequest(@NotNull Task.Status status){} public record Response(UUID id,String title,String description,String category,String priority,LocalDate dueDate,LocalTime dueTime,Integer estimatedMinutes,Integer actualMinutes,UUID goalId,UUID milestoneId,Task.Recurrence recurrence,Task.Status status,Instant createdAt,Instant updatedAt){}
}
