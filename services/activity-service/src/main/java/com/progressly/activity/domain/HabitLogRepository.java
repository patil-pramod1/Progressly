package com.progressly.activity.domain; import java.time.LocalDate; import java.util.*; import org.springframework.data.jpa.repository.JpaRepository; public interface HabitLogRepository extends JpaRepository<HabitLog,UUID>{List<HabitLog> findByHabitIdAndUserIdOrderByLogDateDesc(UUID habitId,UUID userId);Optional<HabitLog> findByHabitIdAndUserIdAndLogDate(UUID habitId,UUID userId,LocalDate date);}
